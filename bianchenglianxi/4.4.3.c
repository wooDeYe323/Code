#include <stdio.h>
#define NAME "xiang"
int main(void)
{
    double cash = 23.03;
    printf("The %s family just may be %.1f dollars richer !",NAME,cash);

    getchar();
    getchar();

    return 0;
}